# cca-plugin-test

Test repository for the GitHub Actions migration plugin running on Copilot Cloud Agent.

The plugin is loaded from [AlexDeMichieli/actions-migrations-via-copilot](https://github.com/AlexDeMichieli/actions-migrations-via-copilot) via `.github/copilot/settings.json`.
